export interface Task {
  id: string;
  title: string;
  status: string;
  project: string;
  assignedTo: string;
  deadline: string;
  priority: string;
  estimate: string;
  consumed: string;
  left: string;
}
